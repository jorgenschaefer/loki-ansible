127.0.0.1       localhost
::1             localhost ip6-localhost ip6-loopback
ff02::1         ip6-allnodes
ff02::2         ip6-allrouters

# Leave the Strato stuff in, else the system won't reboot
{{ ansible_default_ipv4.address }} {{ hostname.fqdn }} {{ hostname.short }} h2610908.stratoserver.net h2610908
{% if ansible_default_ipv6 -%}
{{ ansible_default_ipv6.address }} {{ hostname.fqdn }} {{ hostname.short }}
{%- endif %}
